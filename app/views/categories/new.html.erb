<%= form_for @category do |f| %>
    <%= f.label :"Category Name:" %>
    <%= f.text_field :name,  class: 'form-control' %>
       <div class="d-grid gap-2">
        <%= f.submit 'Create Category', class: 'btn btn-custom' %>
    </div>
<% end %>